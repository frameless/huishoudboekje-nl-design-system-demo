import React from "react";
import DeadEndPage from "../../DeadEndPage";

const Bookings = () => {
	return (
		<DeadEndPage message={"Deze pagina bestaat nog niet."} />
	);
};

export default Bookings;