export enum AfspraakType {
	Expense = "expense",
	Income = "income"
}

export enum AfspraakPeriod {
	Periodic = "periodic",
	Once = "once"
}

export enum IntervalType {
	Day = "day",
	Week = "week",
	Month = "month",
	Year = "year"
}