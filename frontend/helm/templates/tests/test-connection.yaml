apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "huishoudboekje.fullname" . }}-test-connection"
  labels:
    {{- include "huishoudboekje.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "huishoudboekje.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
