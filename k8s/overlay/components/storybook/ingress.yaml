apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hhb-storybook-ingress
  labels:
    name: hhb-storybook-ingress
spec:
  rules:
    - host: sb-huishoudboekje.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hhb-storybook
                port:
                  number: 80
