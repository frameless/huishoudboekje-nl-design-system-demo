apiVersion: apps/v1
kind: Deployment
metadata:
  name: hhb-huishoudboekjeservice
  namespace: ${NAMESPACE}
  labels:
    name: hhb-huishoudboekjeservice
spec:
  replicas: 1
  selector:
    matchLabels:
      name: hhb-huishoudboekjeservice
  template:
    metadata:
      labels:
        name: hhb-huishoudboekjeservice
    spec:
      initContainers:
        - name: init1-wait-for-db
          image: busybox
          command: [ "sh", "-c", "while true; do sleep 1; echo Waiting for database...; if nc -z hhb-database 5432; then echo Database is up!; exit 0; fi; done; echo Aint gonna wait for the database forever...; exit 1" ]
        - name: init2-prepare-db
          image: postgres:13-alpine
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh"]
          args:
            - -c
            - >-
              export PGPASSWORD=postgres &&
              psql -h hhb-database -U postgres -c "CREATE USER hhbsvc WITH PASSWORD 'hhbsvc'" || true &&
              psql -h hhb-database -U postgres -c "CREATE DATABASE huishoudboekjeservice" || true &&
              psql -h hhb-database -U postgres -c "GRANT ALL PRIVILEGES ON DATABASE huishoudboekjeservice TO hhbsvc" || true;
        - name: init3-migrate-db
          image: registry.gitlab.com/commonground/huishoudboekje/app-new/huishoudboekjeservice:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          env:
            - name: HHB_SECRET
              value: ${HHB_SECRET}
            - name: HHB_DATABASE_URL
              value: "postgresql://hhbsvc:hhbsvc@hhb-database/huishoudboekjeservice"
          command: ["python"]
          args: ["manage.py", "db", "upgrade"]
      containers:
        - name: hhb-huishoudboekjeservice
          image: registry.gitlab.com/commonground/huishoudboekje/app-new/huishoudboekjeservice:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
#          resources:
#            limits:
#              cpu: 1000m
#              memory: 256Mi
#            requests:
#              cpu: 100m
#              memory: 128Mi
          env:
            - name: HHB_SECRET
              value: ${HHB_SECRET}
            - name: HHB_DATABASE_URL
              value: "postgresql://hhbsvc:hhbsvc@hhb-database/huishoudboekjeservice"
