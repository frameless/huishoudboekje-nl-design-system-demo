apiVersion: v1
kind: Service
metadata:
  name: hhb-alarmenservice
  labels:
    name: hhb-alarmenservice
spec:
  type: ClusterIP
  selector:
    name: hhb-alarmenservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
