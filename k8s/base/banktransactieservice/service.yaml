apiVersion: v1
kind: Service
metadata:
  name: hhb-banktransactieservice
  labels:
    name: hhb-banktransactieservice
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    name: hhb-banktransactieservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
