apiVersion: v1
kind: Service
metadata:
  name: hhb-signalenservice
  labels:
    name: hhb-signalenservice
spec:
  type: ClusterIP
  selector:
    name: hhb-signalenservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
