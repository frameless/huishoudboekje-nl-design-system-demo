apiVersion: v1
kind: Service
metadata:
  name: hhb-unleashservice
  labels:
    name: hhb-unleashservice
  namespace: ${NAMESPACE}
spec:
  type: ClusterIP
  selector:
    name: hhb-unleashservice
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
