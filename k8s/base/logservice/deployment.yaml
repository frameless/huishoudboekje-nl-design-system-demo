apiVersion: apps/v1
kind: Deployment
metadata:
  name: hhb-logservice
  namespace: ${NAMESPACE}
  labels:
    name: hhb-logservice
spec:
  replicas: 1
  selector:
    matchLabels:
      name: hhb-logservice
  template:
    metadata:
      labels:
        name: hhb-logservice
    spec:
      initContainers:
        - name: init1-wait-for-db
          image: busybox
          command: [ "sh", "-c", "while true; do sleep 1; echo Waiting for database...; if nc -z hhb-database 5432; then echo Database is up!; exit 0; fi; done; echo Aint gonna wait for the database forever...; exit 1" ]
        - name: init2-prepare-db
          image: postgres:13-alpine
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh"]
          args:
            - -c
            - >-
              export PGPASSWORD=postgres &&
              psql -h hhb-database -U postgres -c "CREATE USER logsvc WITH PASSWORD 'logsvc'" || true &&
              psql -h hhb-database -U postgres -c "CREATE DATABASE logservice" || true &&
              psql -h hhb-database -U postgres -c "GRANT ALL PRIVILEGES ON DATABASE logservice TO logsvc" || true;
        - name: init3-migrate-db
          image: registry.gitlab.com/commonground/huishoudboekje/app-new/logservice:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          env:
            - name: HHB_SECRET
              value: ${HHB_SECRET}
            - name: LOG_DATABASE_URL
              value: "postgresql://logsvc:logsvc@hhb-database/logservice"
          command: ["python"]
          args: ["manage.py", "db", "upgrade"]
      containers:
        - name: hhb-logservice
          image: registry.gitlab.com/commonground/huishoudboekje/app-new/logservice:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
#          resources:
#            limits:
#              cpu: 1000m
#              memory: 256Mi
#            requests:
#              cpu: 100m
#              memory: 128Mi
          env:
            - name: HHB_SECRET
              value: ${HHB_SECRET}
            - name: LOG_DATABASE_URL
              value: "postgresql://logsvc:logsvc@hhb-database/logservice"
