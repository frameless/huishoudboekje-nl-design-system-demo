---
apiVersion: v1
kind: Service
metadata:
  name: hhb-keycloak
  labels:
    name: hhb-keycloak
spec:
  type: ClusterIP
  selector:
    name: hhb-keycloak
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
