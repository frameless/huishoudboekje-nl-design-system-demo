issuer: http://localhost:6556

storage:
  type: sqlite3
  config:
    file: /tmp/dex.db

web:
  http: 0.0.0.0:6556

oauth2:
  # use ["code", "token", "id_token"] to enable implicit flow for web-only clients
  #   responseTypes: [ "code" ] # also allowed are "token" and "id_token"
  # By default, Dex will ask for approval to share data with application
  # (approval for sharing data from connected IdP to Dex is separate process on IdP)
  skipApprovalScreen: true
  # If only one authentication method is enabled, the default behavior is to
  # go directly to it. For connected IdPs, this redirects the browser away
  # from application to upstream provider such as the Google login page
  alwaysShowLoginScreen: false
  # Uncommend the passwordConnector to use a specific connector for password grants
  #   passwordConnector: local

expiry:
  idTokens: "168h"

staticClients:
  - id: huishoudboekje
    redirectURIs:
      - http://localhost:3000/api/oidc_callback
    name: 'Huishoudboekje Schaalbaar'
    secret: ZwwcL2zjQyJUBnFPVoOb

enablePasswordDB: true

staticPasswords:
  - username: user@local
    email: user@local
    name: Developer
    userID: 363283b9-07bb-4247-bfe4-977a1e221cc4
    hash: $2y$12$xWWlTptiMyPJbuPo7WwBgefl/sj3vz8iNJn5fPwVWQcBcZh1HsC.W # demo
  - username: koen@openweb.nl
    email: koen@openweb.nl
    name: Koen Brouwer
    userID: a151be49-c956-4834-a511-880b39011959
    hash: $2a$10$Ff5DitVJumqKi/dQkM4x2u4wTQV8LuBxU8KPICKLhv3C32gRJnC/y
