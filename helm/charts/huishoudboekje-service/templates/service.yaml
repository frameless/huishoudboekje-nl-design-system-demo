apiVersion: v1
kind: Service
metadata:
  name: {{ include "huishoudboekje-service.fullname" . }}
  labels:
    {{- include "huishoudboekje-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "huishoudboekje-service.selectorLabels" . | nindent 4 }}
