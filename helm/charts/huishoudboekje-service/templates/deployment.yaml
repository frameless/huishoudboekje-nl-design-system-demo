apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "huishoudboekje-service.fullname" . }}
  labels:
    {{- include "huishoudboekje-service.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "huishoudboekje-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- if eq (include "huishoudboekje-service.imageTag" .) "dev" }}
        dev: {{ randAlphaNum 24 | b64enc | quote}}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "huishoudboekje-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "huishoudboekje-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ include "huishoudboekje-service.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: HHB_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-secrets" (include "huishoudboekje-service.fullname" .) }}
                  key: SECRET_KEY
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: database
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: password
            - name: HHB_DATABASE_URL
              value: "postgresql://$(PGUSER):$(PGPASSWORD)@{{ .Release.Namespace }}-postgresql/$(PGDATABASE)"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /health
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      initContainers:
        - name: {{ printf "%s-db-init" .Chart.Name | quote }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ include "huishoudboekje-service.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: HHB_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ printf "%s-secrets" (include "huishoudboekje-service.fullname" .) }}
                  key: SECRET_KEY
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: database
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: password
            - name: HHB_DATABASE_URL
              value: "postgresql://$(PGUSER):$(PGPASSWORD)@{{ .Release.Namespace }}-postgresql/$(PGDATABASE)"
          command: ["python"]
          args: ["manage.py", "db", "upgrade"]
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
        - name: {{ printf "%s-sample-data" .Chart.Name | quote }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "registry.gitlab.com/commonground/huishoudboekje/app-new/sample_data:{{ include "huishoudboekje-service.imageTag" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: database
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: username
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.service }}-{{ include "huishoudboekje-service.name" . }}
                  key: password
            - name: DATABASE_URL
              value: "postgresql://$(PGUSER):$(PGPASSWORD)@{{ .Release.Namespace }}-postgresql/$(PGDATABASE)"
            - name: DATABASE_NAME
              value: "$(PGDATABASE)"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-secrets" (include "huishoudboekje-service.fullname" .) }}
  labels:
  {{- include "huishoudboekje-service.labels" . | nindent 4 }}
data:
  SECRET_KEY: {{ default (randAlphaNum 24) .Values.secretKey | b64enc | quote }}
