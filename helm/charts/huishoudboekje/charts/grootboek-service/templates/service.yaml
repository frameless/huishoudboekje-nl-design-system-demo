apiVersion: v1
kind: Service
metadata:
  name: {{ include "grootboek-service.fullname" . }}
  labels:
    {{- include "grootboek-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grootboek-service.selectorLabels" . | nindent 4 }}
