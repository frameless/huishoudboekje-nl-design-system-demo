{{- if false -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medewerker-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "medewerker-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medewerker-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
  {{- end -}}