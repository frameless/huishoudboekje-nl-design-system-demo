apiVersion: v1
kind: Service
metadata:
  name: {{ include "organisatie-service.fullname" . }}
  labels:
    {{- include "organisatie-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "organisatie-service.selectorLabels" . | nindent 4 }}
