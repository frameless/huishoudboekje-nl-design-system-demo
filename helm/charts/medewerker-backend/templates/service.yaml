apiVersion: v1
kind: Service
metadata:
  name: {{ include "medewerker-backend.fullname" . }}
  labels:
    {{- include "medewerker-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medewerker-backend.selectorLabels" . | nindent 4 }}
