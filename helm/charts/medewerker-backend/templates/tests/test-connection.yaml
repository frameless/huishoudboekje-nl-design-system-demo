apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "medewerker-backend.fullname" . }}-test-connection"
  labels:
    {{- include "medewerker-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "medewerker-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
