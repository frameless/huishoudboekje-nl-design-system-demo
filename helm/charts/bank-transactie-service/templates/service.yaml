apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-transactie-service.fullname" . }}
  labels:
    {{- include "bank-transactie-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-transactie-service.selectorLabels" . | nindent 4 }}
