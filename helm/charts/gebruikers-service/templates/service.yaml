apiVersion: v1
kind: Service
metadata:
  name: {{ include "gebruikers-service.fullname" . }}
  labels:
    {{- include "gebruikers-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gebruikers-service.selectorLabels" . | nindent 4 }}
