{{- if false -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "organisatie-service.fullname" . }}-test-connection"
  labels:
    {{- include "organisatie-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "organisatie-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end -}}